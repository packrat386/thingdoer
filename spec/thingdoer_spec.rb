require 'spec_helper'

describe Thingdoer do
  it 'has a version number' do
    expect(Thingdoer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
